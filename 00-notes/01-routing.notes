== ROUTING ==
    - based on folder structure, everything in "pages" folder gets rendered as a page

    [javascript](
        router.push('url'); // goes to url
        router.replace('url'); // goes to url, but doesn't allow navigation back. Good for form submissions.
    )[end]